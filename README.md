# Simple ASP.NET Backend

This folder contains a minimal ASP.NET backend example for the Service Day project.

## Endpoints

- GET /api/services
- GET /api/services/1
- POST /api/services

## Notes

This is a simple route/controller/model setup using Global.asax and a basic custom route handler.
